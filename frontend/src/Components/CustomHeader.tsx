import React from 'react';
import {Box, Typography} from "@material-ui/core";

export const CustomHeader = () => {
    return (
        <div>
            <Typography variant='h2'>Blockchain Election App</Typography>
        </div>

    )
}